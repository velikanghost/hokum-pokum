interface Window {
  ethereum?: any; // Using `any` to be more flexible; change to a more specific type if needed
  tron?: any; // Using `any` to be more flexible; change to a more specific type if needed
  tronWeb?: any; // Using `any` to be more flexible; change to a more specific type if needed
  tronLink?: any; // Using `any` to be more flexible; change to a more specific type if needed
}
